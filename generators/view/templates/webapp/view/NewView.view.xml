<mvc:View
	controllerName="<%= NAMESPACE %>.controller.<%= VIEW_NAME %>"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc">
	<Page
		title="{i18n>ViewName.title}"
		class="sapUiResponsiveContentPadding"
		showNavButton="true"
		navButtonPress="onNavBack">
		<content>
			<Text text="{i18n>Home.itWorks}"/>
		</content>
	</Page>
</mvc:View>
